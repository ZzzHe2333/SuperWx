项目背景：
你正在改造 SuperWx / wxauto4，使其适配当前 Windows 微信 4.x UI。
当前已通过 UIAutomation 实测确认以下核心 selector：

main_window:
- ClassName: mmui::MainWindow
- Name 可能为：微信 / Weixin / WeChat

chat_message_page:
- AutomationId: chat_message_page
- ClassName: mmui::ChatMessagePage

message_view:
- ClassName: mmui::MessageView

chat_message_list:
- AutomationId: chat_message_list
- ClassName: mmui::RecyclerListView
- Name: 消息

input_view:
- ClassName: mmui::InputView

chat_input_field:
- AutomationId: chat_input_field
- ClassName: mmui::ChatInputField
- ControlType: EditControl
- 注意：不要用 Name 定位，因为 Name 会变化

send_button:
- Name: 发送
- ClassName: mmui::XOutlineButton
- ControlType: ButtonControl
- 注意：不要把“发送表情”“发送文件”“发送收藏”当作发送消息按钮

session_list:
- AutomationId: session_list
- ClassName: mmui::XTableView
- Name: 会话

通用限制：
1. 不要修改授权、license、auth、加密、网络校验相关代码。
2. 不要删除旧逻辑，只能新增 fallback。
3. 不要一次性跨阶段乱改。
4. 每阶段完成后必须输出：修改了哪些文件、改了什么、测试命令、测试结果、下一阶段是否可继续。
5. 如果某功能无法确认，不要编造，先输出检测脚本和失败原因。


你现在执行第七阶段：增加 doctor 命令和完整回归测试。

前置条件：
前六阶段已经完成。

本阶段目标：
新增统一健康检查命令，方便未来微信 UI 改动后快速定位问题。

修改范围：
- wxauto4/__main__.py 或新增 wxauto4/doctor.py
- tests/manual_test_wechat.py
- tests/regression_wechat_stage7.py

doctor 命令目标：
支持以下任一方式：
python -m wxauto4 doctor
或者：
python tests/probe_wechat_locator.py

doctor 输出：
main_window          OK / FAILED
chat_message_page    OK / FAILED
message_view         OK / FAILED
chat_message_list    OK / FAILED
input_view           OK / FAILED
chat_input_field     OK / FAILED
send_button          OK / FAILED
session_list         OK / FAILED

附加要求：
1. 如果 send_button FAILED，提示：“请先在聊天输入框输入几个字，不要发送，然后重新运行 doctor。”
2. 输出微信窗口 Name、ClassName、Rect。
3. 输出每个控件使用的 selector。
4. 生成 TXT 报告。
5. 不发送消息，不点击发送按钮。

manual_test_wechat.py 目标：
1. 初始化 WeChat。
2. 打印当前聊天控件状态。
3. 读取当前消息数量。
4. 可选 dry_run 测试 SendMsg。
5. 可选 dry_run 测试 SendFiles。
6. 可选 ChatWith。
7. 默认不真实发送消息。

regression 测试顺序：
1. locator probe
2. chatbox locator
3. GetAllMessage
4. ChatWith dry_run
5. SendMsg dry_run
6. SendFiles dry_run
7. Listen dry_run

测试命令：
python -m wxauto4 doctor
python tests/manual_test_wechat.py
python tests/regression_wechat_stage7.py

完成后输出：
1. 新增了哪些测试。
2. doctor 如何运行。
3. 当前各项状态。
4. 哪些功能已支持。
5. 哪些功能需要人工确认。
6. 是否可以进入第八阶段。
